<!DOCTYPE html>
<html>
	<script src='https://api.tiles.mapbox.com/mapbox.js/v2.2.1/mapbox.js'></script>
	<link href='https://api.tiles.mapbox.com/mapbox.js/v2.2.1/mapbox.css' rel='stylesheet' />

<head>
  <title>WesleyVance</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>

<body class="<%= controller_name %> <%= action_name %>">
	 
	<!-- =========================================================================================================== -->
	<!-- 																	THANKS FOR CHECKING OUT MY CODE!  																				 -->
	<!-- FEEL FREE TO TAKE AND SHARE ANY PART JUST LINK BACK TO THIS SITE IN CODE COMMENTS SO OTHERS CAN SEE IT TOO! -->
	<!-- 												IF YOU HAVE QUESITONS EMAIL ME AT WESLEY.VANCE@MAC.COM :)  													 -->
	<!-- =========================================================================================================== -->
	
	<div id="container">

		<% flash.each do |key, value| %>
		  <div class="alert alert-<%= key %> center"><%= value %></div>
		<% end %>
			
		<% if current_page?(controller: '/welcome', action: 'index')%>
		<%else%>
			<% if user_signed_in? %>
				<div class="adminBar links">
			  	<h4>			  		
			  		<%= link_to root_path, :class=>"navBarLinks" do%>
							Home
						<%end%>
						<%= link_to(contents_path(@content, content: {:kind => "twitter"}), :method => :post) do%>
							Tweet
						<%end%>
			  		<%= link_to(destroy_user_session_path, :method => :delete, :class=>"navBarLinks") do%>
			  			Sign Out
			  		<%end%>
			  	</h4>
			  </div>
		  <%end%>
	  <%end%>

		<div class="bodyWrapper">
			<%= yield %>
		</div>
	</div>
	<div id="version" class="links">
		<h6><a href="https://trello.com/b/Xeqqh9j1/blog" target="_blank">Trello: Version .3</a></h6>
	</div>
</body>
</html>
