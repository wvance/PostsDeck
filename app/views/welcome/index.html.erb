<div class="row">
	<!-- MAX WIDTH SLIDER -->
</div>

<div class="row">
	<div class="col-xs-12">
		<h1 class="">Posts Deck</h1>
	</div>
</div>

<div class="row">
	<div class="col-xs-12">
		<h1 class="">Recent Posts</h1><hr>
		<% @active_content.each do |content| %>
			<div class="col-xs-12 col-md-4">
				<%= link_to content do %>
          <%= image_tag content.content_attachments.first.image, :class=> "recentContentPost" if content.content_attachments.present? %>
          <h3><%= content.title.capitalize%></h3>
          <% @content_user = User.where(:id=>content.author).first %>
        <%end%>
				<h4>
					<%= sanitize_markdown(content, 100) %>
				</h4>
				<h6><%= @content_user.first_name%> <%= @content_user.last_name%></h6>
			</div>
		<%end%>
	</div>
</div>

<div class="welcomeContentFooter">
	<div class="col-xs-4">

	</div>
	<div class="col-xs-4">
	</div>
</div>
